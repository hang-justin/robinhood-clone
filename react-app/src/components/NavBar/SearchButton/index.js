import './SearchButton.css';

const SearchButton = () => {

    return (
        <button id='search-button' className='flx-row-align-ctr'>
            <span className="material-symbols-outlined">
                search
            </span>
        </button>
    )
};

export default SearchButton;
